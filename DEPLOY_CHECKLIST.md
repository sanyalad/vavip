# Чеклист для деплоя

## ✅ Перед деплоем

### Подготовка сервера
- [ ] Docker установлен и запущен
- [ ] Docker Compose установлен
- [ ] Git установлен
- [ ] Порты 80 и 5000 свободны
- [ ] Достаточно места на диске (минимум 10GB свободно)

### Настройка переменных окружения

#### Backend (.env файл)
- [ ] `SECRET_KEY` - сгенерирован уникальный секретный ключ
- [ ] `JWT_SECRET_KEY` - сгенерирован уникальный JWT ключ
- [ ] `DATABASE_URL` - настроен URL PostgreSQL (для Docker: `postgresql://vavip:PASSWORD@db:5432/vavip`)
- [ ] `REDIS_URL` - настроен URL Redis (`redis://redis:6379/0`)
- [ ] `CORS_ORIGINS` - указаны production домены
- [ ] API ключи платежных систем (если используются)
- [ ] Настройки email (если используются)

#### Frontend (.env файл)
- [ ] `VITE_API_URL` - настроен URL API (обычно `/api` для production)
- [ ] `VITE_WS_URL` - настроен WebSocket URL (если используется)
- [ ] Ключи аналитики (если используются)
- [ ] Ключи карт (если используются)

#### Docker Compose
- [ ] `POSTGRES_PASSWORD` - установлен пароль для PostgreSQL
  - Можно установить через переменную окружения: `export POSTGRES_PASSWORD=your-password`
  - Или создать `.env` файл в корне проекта с `POSTGRES_PASSWORD=your-password`

### Безопасность
- [ ] Все секретные ключи изменены с дефолтных значений
- [ ] Права доступа к .env файлам настроены (chmod 600)
- [ ] Firewall настроен (если необходимо)
- [ ] SSL сертификаты подготовлены (если используется HTTPS)

## 📋 Процесс деплоя

### Первоначальный деплой
1. [ ] Клонирован репозиторий
2. [ ] Скрипты сделаны исполняемыми (`chmod +x *.sh`)
3. [ ] Настроены .env файлы
4. [ ] Запущен `./deploy.sh` или `.\deploy.ps1`
5. [ ] Проверен статус контейнеров
6. [ ] Проверены логи
7. [ ] Проверена работа frontend (http://localhost)
8. [ ] Проверена работа backend API (http://localhost:5000/api/health)

### После деплоя
- [ ] Выполнены миграции базы данных (если необходимо)
- [ ] Создан первый backup базы данных
- [ ] Настроен мониторинг (опционально)
- [ ] Проверена работа всех функций приложения

## 🔄 Обновление деплоя

- [ ] Проверены изменения в репозитории
- [ ] Создан backup базы данных (перед обновлением)
- [ ] Запущен `./deploy-update.sh` или `.\deploy-update.ps1`
- [ ] Проверен статус контейнеров после обновления
- [ ] Проверены логи на наличие ошибок
- [ ] Проверена работа приложения

## 🧹 Очистка сервера

### Регулярная очистка (раз в месяц)
- [ ] Запущен `./server-clean-prod.sh` для безопасной очистки
- [ ] Проверено использование диска (`docker system df`)
- [ ] Удалены старые логи (если необходимо)

### Перед полным передеплоем
- [ ] Запущен `./deploy-clean.sh` для полной очистки
- [ ] Проверено что все данные сохранены

## 📝 Важные заметки

### Генерация секретных ключей

**Linux/macOS:**
```bash
python3 -c "import secrets; print(secrets.token_hex(32))"
```

**Windows PowerShell:**
```powershell
[Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))
```

### Backup базы данных

**Создание backup:**
```bash
docker-compose -f docker-compose.prod.yml exec db pg_dump -U vavip vavip > backup_$(date +%Y%m%d_%H%M%S).sql
```

**Восстановление из backup:**
```bash
docker-compose -f docker-compose.prod.yml exec -T db psql -U vavip vavip < backup_YYYYMMDD_HHMMSS.sql
```

### Настройка POSTGRES_PASSWORD

POSTGRES_PASSWORD используется в `docker-compose.prod.yml` для пароля базы данных.

**Вариант 1: Переменная окружения**
```bash
export POSTGRES_PASSWORD=your-secure-password
docker-compose -f docker-compose.prod.yml up -d
```

**Вариант 2: .env файл в корне проекта**
Создайте `.env` файл в корне проекта:
```
POSTGRES_PASSWORD=your-secure-password
```

**Вариант 3: Прямое редактирование docker-compose.prod.yml**
Отредактируйте строку 66 в `docker-compose.prod.yml`:
```yaml
- POSTGRES_PASSWORD=your-secure-password
```

⚠️ **ВАЖНО:** Используйте тот же пароль что указан в `backend/.env` в `DATABASE_URL`!

## 🆘 Решение проблем

Если что-то пошло не так:

1. Проверьте логи:
   ```bash
   docker-compose -f docker-compose.prod.yml logs
   ```

2. Проверьте статус контейнеров:
   ```bash
   docker-compose -f docker-compose.prod.yml ps
   ```

3. Проверьте .env файлы на наличие ошибок

4. Проверьте что порты не заняты:
   ```bash
   netstat -tuln | grep -E ':(80|5000)'
   ```

5. Проверьте использование диска:
   ```bash
   docker system df
   df -h
   ```

## 📚 Дополнительная документация

- Подробная документация: [DEPLOY.md](DEPLOY.md)
- Быстрый старт: [DEPLOY_QUICKSTART.md](DEPLOY_QUICKSTART.md)

